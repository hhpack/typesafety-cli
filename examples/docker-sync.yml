version: 2
options:
  verbose: true

syncs:
  typesafety-sync:
    src: '..'
    notify_terminal: true
    sync_userid: '1000'
    sync_strategy: 'rsync'
    sync_host_port: 10871
    sync_excludes_type: Path
    sync_excludes:
      - _build
      - .git
      - examples/docker-compose-dev.yml
      - examples/docker-compose.yml
      - examples/docker-sync.yml
      - examples/Dockerfile
      - examples/typesafetysudo
      - setup.data
      - setup.log
      - test.native
      - typesafety.native
