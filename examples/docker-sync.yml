options:
  verbose: true

syncs:
  typesafety-sync:
    src: '../'
    dest: '/home/ocaml'
    sync_strategy: 'rsync'
    sync_host_ip: 127.0.0.1
    sync_host_port: 10871
    sync_excludes: ['Test.native','Typesafety.native']
